name: "AI Review"

permissions:
  issues: write
  pull-requests: write
  contents: read

on:
  pull_request:
    types: [opened, reopened, ready_for_review, review_requested]
  issue_comment:

jobs:
  ai-review:
    permissions:
      issues: write
      pull-requests: write
      contents: read
    uses: stuhlmuller/workflows/.github/workflows/review-bot.yml@33a38c3df533ef6958350989befea9c882f97deb # main
    secrets: inherit
