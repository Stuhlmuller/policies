name: "AI Review"

permissions:
  issues: write
  pull-requests: write
  contents: read

on:
  pull_request:
    types: [opened, reopened, ready_for_review, review_requested]
  issue_comment:

jobs:
  ai-review:
    permissions:
      issues: write
      pull-requests: write
      contents: read
    uses: stuhlmuller/workflows/.github/workflows/review-bot.yml@d26688db16a42f70710c90ab307abcbfb618c63b # main
    secrets: inherit
