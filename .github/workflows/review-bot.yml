name: "AI Review"

permissions:
  issues: write
  pull-requests: write
  contents: read

on:
  pull_request:
    types: [opened, reopened, ready_for_review, review_requested]
  issue_comment:

jobs:
  ai-review:
    permissions:
      issues: write
      pull-requests: write
      contents: read
    uses: stuhlmuller/workflows/.github/workflows/review-bot.yml@9404d2a4f865af2d7241c5722b80beb837fbd600 # main
    secrets: inherit
