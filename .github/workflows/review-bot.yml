name: "AI Review"

permissions:
  issues: write
  pull-requests: write
  contents: read

on:
  pull_request:
    types: [opened, reopened, ready_for_review, review_requested]
  issue_comment:

jobs:
  ai-review:
    permissions:
      issues: write
      pull-requests: write
      contents: read
    uses: stuhlmuller/workflows/.github/workflows/review-bot.yml@43e92f32a7b6175ed6e5ee05a66c432745989ded # main
    secrets: inherit
